/* "use client"

import { useParams } from "next/navigation"
import { useRouter } from "next/router"

const LittleHeading = () => {
    const params = useParams()
  
    return (
    <div>littleHeading {params}</div>
  )
}

export default LittleHeading */